{{- if .Values.enketo.autoscaling.enabled }}
apiVersion: autoscaling/v2beta1
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "kobo.fullname" . }}-enketo
  labels:
    {{- include "kobo.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "kobo.fullname" . }}-enketo
  minReplicas: {{ .Values.enketo.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.enketo.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.enketo.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        targetAverageUtilization: {{ .Values.enketo.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.enketo.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        targetAverageUtilization: {{ .Values.enketo.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
