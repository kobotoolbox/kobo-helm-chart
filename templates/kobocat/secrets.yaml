apiVersion: v1
kind: Secret
metadata:
  name: {{ include "kobo.fullname" . }}-kobocat
  labels:
    {{- include "kobo.labels" . | nindent 4 }}
type: Opaque
data:
{{- range $k, $v := .Values.kobocat.env.secret }}
  {{ $k }}: {{ $v | b64enc | quote }}
{{- end }}
